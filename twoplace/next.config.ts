import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['lh3.googleusercontent.com'], // Buraya izin verdiğiniz dış domain'leri ekleyin
  },
};

export default nextConfig;
